project:
  name: "{{projectName}}"
  language: "{{language}}"
  description: ""

agents:
  delegation_first: true
  model_routing:
    low: "low"
    medium: "medium"
    high: "high"
  providers: {}

skills:
  directories:
    - ".harness/skills"
  auto_detect: true

workflows:
  verification:
    checks:
      - build
      - test
      - lint

templates:
  agents_md:
    variant: "{{variant}}"
